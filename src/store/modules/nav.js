import nav from '@/data/nav.js';

const state = {
    nav: nav
};

const mutations = {
    
};

const actions = {
    
};

const getters = {
    nav: state => {
        return state.nav;
    }
};

export default {
    state,
    mutations,
    actions,
    getters
};